// !DOM IF
// if(konsdisi (jika kondsi bernlai true makan aksi akan dijalankan)) {
//     aksi
// }
// ... Jika false maka akan keluar dari pengkondisian

// ? Example
// let angka = 2;

// if (angka === 1) {
//   alert(`anda memasukan angka 1`);
// }

// // Jika kondisi false
// alert(`angka tidak sama`);

// let angka = 2;
// if (angka < 5) {
//   alert(`${angka} lebih kecil dari 5`);
// }

// ? example 2 menjari bilangan genap dan ganjil

let angka = 2;
if (angka % 2 == 0) {
  alert(`${angka} adalah bilangan genap`);
}

// let angka = prompt(`masukan angka`);

// if (angka % 2 == 0) {
//   alert(`${angka} adalah bilangan genap`);
// } else {
//   alert(`${angka} adalah bilangan ganjil`);
// }

// !DOM IF Else
// if(konsdisi (jika kondsi bernlai true makan aksi akan dijalankan)) {
//     aksi
// }else {
//  Aksi (Jika kondisi if tidak terpenuhi maka else akan dijalankan)
// }
